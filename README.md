# unordiff
unordiff(Unordered Diff) is a lightweight tool designed for comparing JSON and YAML configurations with a focus on content, rather than order. Whether you’re dealing with nested objects or arrays, unordiff helps you see the meaningful differences without being distracted by the sequence of elements.
